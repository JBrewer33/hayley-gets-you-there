# Publish only the live site: index.html + the logo + the custom-domain CNAME.
# Everything else (docs/, CLAUDE.md, README.md, etc.) stays local.
*
!.gitignore
!index.html
!CNAME
!assets/
assets/*
!assets/logo.svg
